defaults:
 - default.yaml

viz_type: causal_gt
pane_type: causal_scenario_gt  # ground-truth causal agents, sourced from causal_labels_path JSON (no model outputs)
visualizer:
  _target_: controlledshifts.utils.scenario_visualizers.scenario.ScenarioVisualizer
  config:
    panes_to_plot:
      - ALL_AGENTS
      - CAUSAL_AGENTS_GT
