defaults:
 - default.yaml

# viz_type drives the per-type data prep. render (static/animated) is derived from the visualizer's is_animated
# property; pane_type is derived from viz_type unless explicitly overridden (see viz_non_background).
viz_type: regular         # regular | scored | trajpred | model_output

visualizer:
  _target_: controlledshifts.utils.scenario_visualizers.scenario.ScenarioVisualizer
