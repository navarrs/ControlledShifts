defaults:
 - default.yaml

viz_type: model_output
pane_type: causal_scenario  # override: causal predictions are model output but get their own folder
visualizer:
  _target_: controlledshifts.utils.scenario_visualizers.scenario.ScenarioVisualizer
  config:
    panes_to_plot:
      - ALL_AGENTS
      - CAUSAL_AGENTS_GT
      - CAUSAL_AGENTS_PRED
